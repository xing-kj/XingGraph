# Render Blueprint for XingGraph
#
# Deploy: Click "Deploy to Render" button or:
#   render blueprint launch

services:
  - type: web
    name: xinggraph-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    plan: standard
    envVars:
      - key: LLM_API_KEY
        sync: false
      - key: LLM_MODEL
        value: openai/gpt-4o-mini
      - key: LLM_PROVIDER
        value: openai
      - key: ENV
        value: prod
      - key: DB_PROVIDER
        value: postgres
      - key: DB_HOST
        fromDatabase:
          name: xinggraph-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: xinggraph-db
          property: port
      - key: DB_USERNAME
        fromDatabase:
          name: xinggraph-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: xinggraph-db
          property: password
      - key: DB_NAME
        fromDatabase:
          name: xinggraph-db
          property: database
      - key: VECTOR_DB_PROVIDER
        value: pgvector
      - key: CORS_ALLOWED_ORIGINS
        value: "*"
    disk:
      name: xinggraph-data
      mountPath: /data
      sizeGB: 10

databases:
  - name: xinggraph-db
    plan: starter
    postgresMajorVersion: 17
    ipAllowList: []
