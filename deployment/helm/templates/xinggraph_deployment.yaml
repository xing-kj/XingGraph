apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-xinggraph
  labels:
    app: {{ .Release.Name }}-xinggraph
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-xinggraph
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-xinggraph
    spec:
      containers:
        - name: xinggraph
          image: {{ .Values.xinggraph.image }}
          ports:
            - containerPort: {{ .Values.xinggraph.port }}
          env:
            - name: ENABLE_BACKEND_ACCESS_CONTROL
              value: "false"
            - name: ENV
              value: {{ .Values.xinggraph.env.ENV }}
            - name: PYTHONPATH
              value: {{ .Values.xinggraph.env.PYTHONPATH }}
            - name: VECTOR_DB_PROVIDER
              value: pgvector
            - name: DB_HOST
              value: {{ .Release.Name }}-postgres
            - name: DB_PORT
              value: "{{ .Values.postgres.port }}"
            - name: DB_NAME
              value: {{ .Values.postgres.env.POSTGRES_DB }}
            - name: DB_USERNAME
              value: {{ .Values.postgres.env.POSTGRES_USER }}
            - name: DB_PASSWORD
              value: {{ .Values.postgres.env.POSTGRES_PASSWORD }}
            - name: LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-llm-api-key
                  key: LLM_API_KEY
            - name: LLM_MODEL
              value: {{ .Values.xinggraph.env.LLM_MODEL }}
            - name: LLM_PROVIDER
              value: {{ .Values.xinggraph.env.LLM_PROVIDER }}
          resources:
            limits:
              cpu: {{ .Values.xinggraph.resources.cpu }}
              memory: {{ .Values.xinggraph.resources.memory }}
