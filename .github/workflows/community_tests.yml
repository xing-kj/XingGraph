name: Community Tests (No Secrets)

# Runs on ALL pull requests, including forks.
# No secrets required — only fully mocked unit tests and code quality checks.
# This gives community contributors fast feedback on their changes.

on:
  pull_request:
    branches: [ main, dev ]
    types: [opened, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: community-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  RUNTIME__LOG_LEVEL: ERROR
  ENV: 'dev'

jobs:
  code-quality:
    name: Code Quality
    runs-on: ubuntu-22.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Validate uv lockfile
        run: uv lock --check || { echo "'uv lock --check' failed. Run 'uv lock' and push your changes."; exit 1; }

      - name: Run pre-commit hooks
        uses: pre-commit/action@v3.0.1

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Ruff check
        run: uv run ruff check .

      - name: Ruff format check
        run: uv run ruff format --check .

      - name: Ty type check
        run: uv run ty check .

  unit-tests:
    name: Unit Tests (Mocked, No Secrets)
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11.x']
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install dependencies
        # postgres is needed so the SQL cache adapter tests run instead of skipping.
        run: uv sync --extra dev --extra postgres

      - name: Run mocked unit tests
        run: |
          uv run pytest \
            xinggraph/tests/unit/processing/ \
            xinggraph/tests/unit/entity_extraction/ \
            xinggraph/tests/unit/modules/graph/ \
            xinggraph/tests/unit/modules/chunking/ \
            xinggraph/tests/unit/modules/data/ \
            xinggraph/tests/unit/modules/retrieval/ \
            --ignore=xinggraph/tests/unit/modules/retrieval/graph_completion_retriever_cot_test.py \
            --ignore=xinggraph/tests/unit/modules/retrieval/temporal_retriever_test.py \
            --ignore=xinggraph/tests/unit/modules/retrieval/triplet_retriever_test.py \
            xinggraph/tests/unit/modules/search/ \
            xinggraph/tests/unit/modules/pipelines/ \
            xinggraph/tests/unit/modules/visualization/ \
            xinggraph/tests/unit/modules/ontology/ \
            xinggraph/tests/unit/modules/observability/ \
            xinggraph/tests/unit/modules/users/ \
            xinggraph/tests/unit/modules/memify_tasks/ \
            xinggraph/tests/unit/interfaces/graph/ \
            xinggraph/tests/unit/infrastructure/databases/cache/ \
            xinggraph/tests/unit/infrastructure/databases/vector/test_vector_db_config.py \
            xinggraph/tests/unit/infrastructure/databases/relational/ \
            xinggraph/tests/unit/infrastructure/databases/test_unified_store_engine.py \
            xinggraph/tests/unit/infrastructure/session/ \
            xinggraph/tests/unit/infrastructure/llm/ \
            xinggraph/tests/unit/infrastructure/test_rate_limiting_retry.py \
            xinggraph/tests/unit/tasks/storage/test_add_data_points.py \
            xinggraph/tests/unit/shared/ \
            -v --tb=short

  cli-unit-tests:
    name: CLI Unit Tests (Mocked, No Secrets)
    runs-on: ubuntu-22.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11.x'

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run CLI unit tests
        run: uv run pytest xinggraph/tests/cli_tests/cli_unit_tests/ -v --tb=short

  telemetry-test:
    name: Telemetry Test (Mocked, No Secrets)
    runs-on: ubuntu-22.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11.x'

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run telemetry test
        run: uv run pytest xinggraph/tests/test_telemetry.py -v --tb=short

  notify:
    name: Community Tests Status
    needs: [code-quality, unit-tests, cli-unit-tests, telemetry-test]
    runs-on: ubuntu-latest
    if: ${{ !cancelled() }}
    steps:
      - name: Check Status
        run: |
          if [[ "${{ needs.code-quality.result }}" == "success" &&
                "${{ needs.unit-tests.result }}" == "success" &&
                "${{ needs.cli-unit-tests.result }}" == "success" &&
                "${{ needs.telemetry-test.result }}" == "success" ]]; then
            echo "All community tests passed!"
          else
            echo "One or more community tests failed."
            exit 1
          fi
