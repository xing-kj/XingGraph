# Demo images (效果图)

Real screenshots referenced from the root `README.md`「一个例子看懂它」section,
rendered via relative paths:

- `graph-demo-1.png` ~ `graph-demo-5.png` — 知识图谱效果图（你实际看到的图谱）
- `qa-demo-1.png` ~ `qa-demo-7.png` — 一次真实问答的检索链路截图

> 建议统一宽度（如 1600×900）再放入；README 中已按 `width="720px"` 展示，GitHub 点击可看原图。
> 竞品对比图见 `compare/` 子目录。