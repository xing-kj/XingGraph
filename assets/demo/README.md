# Demo images (效果图)

Place your two screenshots here so the README can render them via relative paths:

- `graph-demo.png` — knowledge graph visualization (图谱展示图)
- `qa-demo.png` — a real answer + retrieval trace (问答示例图)

Referenced from the root `README.md` in the "One Example to See It / 一个例子看懂它" section.